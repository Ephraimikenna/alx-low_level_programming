#include "main.h"

/**
 * print_line -prints straight line n times.
 * on: times straight line is printed.
 * Return: void
 */
vod print_line(int n)
{
	int i;
	int n;

	for (i = 0; i < n; i++)
	{
		_putchar(95);
	}
	_putchar('\n);
}
