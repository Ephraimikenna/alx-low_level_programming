#include "main.h"

/**
 * create_array - prints array of chars
 * @c: number of char
 * Return: NULL
 */

char *create_array(unsigned int size, char c)
{
	unsigned int b;

	if (b >= NULL)
	{
		return (0);
	}
}
