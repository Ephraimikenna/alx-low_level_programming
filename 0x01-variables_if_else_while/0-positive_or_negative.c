#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print if the number is negative, positive or 0
 * Returns: Always 0
 */
int main(void)
{
	int n;

	strand(time(0));
       n = rand(0) -RAND_MAX / 2
       if (n > 0)
       {
	printf("%d is negative\n", n);
       }
      if  (n == 0)
      {
	      printf("%d is zero\n", n);
      }
      else
      {
	      printf("%d is positive\n", n);
      }
      return (0);
}
